[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ccdl-core"
version = "0.1.0"
description = "Backend-neutral Python runtime and protocols for CCDL."
requires-python = ">=3.10"
dependencies = []
